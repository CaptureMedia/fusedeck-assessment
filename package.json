{
  "name": "fusedeck-assessment",
  "version": "1.0.0",
  "main": "dist/app.js",
  "repository": "git@git.capturemedia.ch:fusedeck/fusedeck-assessment.git",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "build": "./node_modules/.bin/tsc -p tsconfig.json",
    "start": "node dist/app.js",
    "start-dev": "./node_modules/.bin/nodemon --ext ts,json --watch './src/**/*.ts' --exec 'yarn build ; yarn start'"
  },
  "dependencies": {
    "better-sqlite3": "^7.4.4",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.4.1",
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.13",
    "nodemon": "^2.0.15",
    "typescript": "^4.4.4"
  }
}
